# Shell Scripts

This folder contains simple Bash scripts created as part of my Linux Learning POC.
## Scripts

* `basic.sh` — Displays basic system information
* `file-management.sh` — Creates, writes, copies, and lists files
* `system-info.sh` — Displays user, directory, kernel, and shell information

These scripts are kept simple for beginner-level practice.
